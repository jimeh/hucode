fix(hardening): close final aggregate review gaps

Preserve hosted Omni unload across both directions of web protocol skew and
prevent dismissed Create Worktree ref loading from emitting an unhandled
rejection.
