feat: add serve-web Omni shell route

Adds `hucode serve-web --omni` root-route switching and stable `/omni` and
`/workbench` web routes. The new `/omni` route serves an initial Hucode-owned
Projects shell with the regular workbench hosted in an iframe.
