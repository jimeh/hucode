fix(build): source built-in extensions from GitHub

Hucode now uses the upstream release artifacts that match its pinned built-in
extension checksums while keeping Open VSX available for user-installed extensions.
