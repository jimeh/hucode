fix: keep Hucode serve-web assets aligned

Hucode web server archives now keep browser and server product metadata aligned
so built-in themes and other extension resources load correctly in
`hucode serve-web`.
