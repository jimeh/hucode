feat(migration): add the reusable editor setup import command

Add the desktop Omni import flow for choosing another editor and profile,
reviewing differences, confirming extension publishers, applying the import,
and recovering, retrying, reporting, or rolling back durable operations.
