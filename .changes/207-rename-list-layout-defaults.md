feat(omni): rename two-line list layout values to default

Rename the default Omni workbench and worktree list layout value from
`twoLine` to `default`, with migration and runtime compatibility for existing
settings.
