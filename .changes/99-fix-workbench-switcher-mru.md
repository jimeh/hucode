fix(omni): restore MRU workbench picker ordering

Workbench quick pickers now order project worktrees and arbitrary workbenches
by activation recency within each lifecycle section and show arbitrary
workbench names and paths on separate lines.
