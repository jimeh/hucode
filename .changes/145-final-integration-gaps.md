fix(hardening): close final integration gaps

Roll back abandoned hosted-workspace shutdown preparation, preserve newly
created worktrees through initial discovery failures, keep asynchronous local
clipboard fallback scoped, and hard-bound update dispatch subprocesses.
