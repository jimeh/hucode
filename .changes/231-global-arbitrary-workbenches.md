feat(omni): persist arbitrary workbenches globally

Arbitrary workbenches in the Omni sidebar now persist in the shared project
catalog, so closing one Omni window no longer loses them and every Omni window
uses the same ordered list.
