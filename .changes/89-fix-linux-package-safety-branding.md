fix(linux): make desktop packages safe and correctly branded

Linux desktop packages now use Hucode metadata and icons without changing
system package repositories or signing keys during install or removal.
