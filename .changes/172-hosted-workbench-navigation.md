fix(omni): restore hosted workbench navigation

Restore workbench switching and command availability inside desktop and
serve-web hosted workbenches while retaining the least-authority shell
capability boundary.
