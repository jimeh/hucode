feat(migration): discover VS Code and Cursor profiles

Add a desktop-only, read-only service that discovers settings, keybindings,
snippets, and installed extension manifests from VS Code, VS Code Insiders,
and Cursor profiles.
